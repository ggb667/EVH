AUDIENCE: EVERYONE
BRANCH: main
WORKTREE: /home/ggb66/dev/EVH
BRANCH_VERIFIED: yes
STATUS: HOLD
PUSH_STATUS: uncommitted_local_changes
APPROVALS: none recorded
FILES_PLANNED: coordination/routing only: Rarity/AJ routing letters answered; no implementation, DB cleanup, staging, or commit assigned
FILES_TOUCHED: pony/team.coordination/twi.status.md, pony/team.coordination/twi.todo.md, pony/team.coordination/twi.decisions.md, pony/work/coordinator-twi.md, pony/memory/twi.md, pony/team.coordination/rd.status.md, pony/work/rd.md, pony/memory/rd.md, pony/team.coordination/rarity.status.md, pony/work/rarity.md, pony/memory/rarity.md, pony/team.coordination/twi.event.stream.history.md
BLOCKERS: preflight: coordinator worktree is dirty; Twilight must reconcile or put away local changes before normal coordination work
NEXT_STEP: launch Twilight in dirty-fix-first mode, put away or reconcile the pending changes, then continue with normal coordination work
QUESTIONS_FOR_TWI: answered 2026-08-02 18:18 EDT: Pinkie/FS/Spike routing letters answered. Pinkie keeps waiting on backend/RDS recovery plus non-destructive SELECT/live endpoint checks and AJ live endpoint; FS remains parked on `pony/fs/main` pending merge/rebase or explicit push instruction, no force-push; Spike remains idle on completed docs handoff with no new docs/routing task. Prior FS/AJ/Rarity/RD answers from 2026-07-31 remain in force.
DECISION_NEEDED: user/Twilight must explicitly assign Rarity label-routing before implementation. User/deployment owner must provide AJ exact live backend/router entrypoint + DB env wiring and confirm safe non-destructive SELECT before AJ validation/wiring. Writable git metadata and confirmed scope are required before staging/committing `scripts/gmail/daily_email_summary.py` and `deploy/evh-lambda.zip`. Deployment owner should repair cbcdvm/ggb667 AWS OAuth secret formatting/mismatch if those accounts are resumed.
LAST_COORDINATION_UPDATE: 2026-08-04 Rarity/AJ routing letters answered; Twilight branch metadata drift corrected to `pony/twi/main`; no secret contents recorded.

AJ_FS_ROUTING_RECHECK_2026_07_24: Applejack and Fluttershy letters rechecked against local state. Live coordinator checkout is `pony/twi/main`; stale local metadata had still listed Twilight as `main` in registry/status/workfile/todo, now corrected. This coordinator context does not retarget workers. AJ remains `pony/aj/main` with route module ready at `pony/worktrees/aj/scripts/rag_document_search_api.py`; root/AJ search found no live backend/router entrypoint beyond Pinkie standalone UI lambda/static caller, so blocker remains user/deployment-provided entrypoint plus DB env wiring. FS remains `pony/fs/main`, Vet Terms, local commit `ac7793c` push-blocked by non-fast-forward; no new FS task and no force-push by default.

ROUTING_RECHECK_2026_07_27: RD branch/memory/workfile `pony/rd/main` is authoritative for Rainbow Dash; `pony/twi/main` belongs to Twilight coordinator context only and is not an RD retarget. FS remains `pony/fs/main` Vet Terms standby with commit `ac7793c` push-blocked non-fast-forward until merge/rebase or explicit push instruction. AJ remains route-module-ready/wiring-blocked because no exact deployed backend/router entrypoint or DB env wiring is exposed locally. Rarity remains `SHUTDOWN_SAVED_PARKED_NO_ACTIVE_MAIL_TASK`; stale memory reference to an active Gmail sender-category guesser is superseded by authoritative status and refreshed.

RD_RDS_BACKEND_DOWN_2026_07_27: pms_page_chunk export attempt crashed the RDS backend; current connections to evh-vector-pg fail with 'the database system is not accepting connections' and 'Hot standby mode is disabled'. Cleanup/export-import is blocked until evh-vector-pg recovers and accepts connections again. Owner: deployment/DB/RDS operator plus user/operator for recovery/maintenance-window decisions. Missing artifact: confirmed healthy RDS accepting connections and a successful non-destructive connection/SELECT check. Do not truncate, rebuild, import, or rerun cleanup yet. Preserve current logs/export artifacts/checkpoints. Wait for evh-vector-pg recovery, then verify non-destructive connectivity first; only after explicit recovery confirmation should RD reassess the cleanup/import plan.

AJ_PINKIE_BACKEND_DOWN_UPDATE_2026_07_27: AJ confirmed route-module-ready/wiring-blocked remains parked with no truncate/import/rebuild attempts; Pinkie confirmed endpoint-validation expectations are paused. Active blocker: evh-vector-pg/RDS is not accepting connections after the pms_page_chunk export attempt; hot standby is disabled. DB-backed route/live validation stays blocked until RDS recovery plus non-destructive connectivity verification. No truncate/import/rebuild attempts are authorized.

RD_RDS_RECOVERY_MONITORING_ROUTE_2026_07_27: evh-vector-pg / pms_page_chunk export fallout: RDS has storage-full / not-accepting-connections history after the export attempt, with hot standby disabled previously reported. Cleanup remains blocked until a non-destructive SELECT succeeds again. Owner: deployment/DB/RDS operator plus user/operator for recovery/maintenance-window decisions. Missing artifact: confirmed healthy RDS accepting connections and successful non-destructive SELECT. Keep routing centered on RDS recovery monitoring, or shell-only local artifact inspection/help if requested. Do not truncate, import, rebuild, vacuum, or run DB cleanup yet. Preserve logs/export artifacts/checkpoints and wait for RDS recovery verification before reassessing cleanup. RD branch/workfile `pony/rd/main` remains authoritative; `pony/twi/main` is coordinator/launcher context only.

PINKIE_ENDPOINT_VALIDATION_PAUSE_REFINED_2026_07_27: evh-vector-pg has storage-full / not-accepting-connections history after the pms_page_chunk export fallout; Pinkie will rerun UI validation only after backend health is confirmed, non-destructive SELECT/live endpoint checks pass, and AJ has a live endpoint. Pinkie performs no DB cleanup actions.

AJ_RECOVERY_MONITORING_ONLY_REFINEMENT_2026_07_27: DB-backed route/live validation is recovery-monitoring only. evh-vector-pg has storage-full / not-accepting-connections history after pms_page_chunk export fallout, and no validation should occur until a non-destructive SELECT succeeds. No truncate/import/vacuum/rebuild attempts are authorized.

RD_ROUTING_CHECK_2026_07_28: Rainbow Dash asked whether launcher/coordinator-routing context should retarget RD. Answer: no retarget. RD stays on authoritative `pony/rd/main` with status `BLOCKED_RDS_RECOVERY_MONITORING_NO_DB_CLEANUP_AUTHORIZED`; the mismatch is stale launcher/coordinator context plus stale RD memory capsule size-probe text, not a new assignment. Continue RDS recovery monitoring only, or shell-only local artifact/log/disk inspection if explicitly requested. No truncate/import/rebuild/vacuum/DB cleanup until evh-vector-pg accepts connections and a non-destructive SELECT succeeds. RD was sent `/tell` id `c4da4fca-e44d-4a2a-aa57-2dcb9a3bea6d` to refresh memory/workfile from authoritative rd.status/Twilight coordination.

RARITY_BRANCH_MISMATCH_2026_07_28: Rarity status/registry branch `pony/rarity/main` is authoritative. The `pony/twi/main` labels in Rarity workfile/memory were stale Twilight coordinator-context contamination; refreshed Rarity workfile/memory to `pony/rarity/main`. Do not update `rarity.status.md` to match the stale workfile label. Rarity remains `SHUTDOWN_SAVED_PARKED_NO_ACTIVE_MAIL_TASK` with no active mail task.

AJ_ROUTING_CHECK_2026_07_28: Applejack status `ROUTE_MODULE_READY_WIRING_BLOCKED_RDS_RECOVERY_MONITORING` on `pony/aj/main` is authoritative, not a launcher problem. No exact live backend/router entrypoint or DB env wiring is exposed locally; evh-vector-pg/RDS recovery plus a successful non-destructive SELECT remain required before DB-backed validation. Keep AJ parked/recovery-monitoring; no DB cleanup/truncate/import/vacuum/rebuild is authorized.

Rarity/AJ tell ids 2026-07-28: Rarity `9b3602cd-1c43-4147-bac9-644f3df4f44a`; AJ `f97aa2db-dc63-42b0-a2e7-afe4d8451885`.
GIT_METADATA_WRITE_BLOCKER_2026_08_02: Verified root and RD linked-worktree files are writable in the workspace, but Git metadata paths `.git`, `.git/index`, `.git/worktrees/rd`, and `.git/worktrees/rd/index` report not-writable in this managed Twilight context. RD commit/push cannot run here because `git add` needs `/home/ggb66/dev/EVH/.git/worktrees/rd/index.lock`; run commit/push from a writable shell or relaunch with git-metadata write access. This is a sandbox/permission-profile blocker, not proof the project worktree itself is broken.
AGENIC_DOC_CHECK_GIT_METADATA_2026_08_02: Source-system docs/code checked after user request. Docs confirm ordinary workers use linked worktrees under `pony/worktrees/<slug>/` and launcher adds project root with `--add-dir` for shared pony state; docs also classify correct-route/write-failure cases as permission blockers. Local EVH managed profile still makes `.git` and `.git/worktrees/rd` not writable, so RD commit/push is blocked by sandbox Git metadata permissions, not by absence of a worker worktree. Source-system follow-up candidate: launcher/profile needs a commit-capable Git metadata lane if ponies are expected to stage/commit/push directly.
CELESTIA_ROUTED_GIT_METADATA_FIX_2026_08_02: Twilight sent Celestia `/tell` id 5e6e8dfa-c60a-464e-8d82-e2d6a8deddf1 requesting source-governance/launcher help for commit-capable worker sessions. Exact issue: worker linked worktree files are writable but target project Git metadata `.git` / `.git/worktrees/<slug>` is read-only in managed Codex sessions, so `git add` cannot create `index.lock`; need durable launcher/profile fix or documented commit-capable lane granting safe write access to target project git common dir plus linked-worktree gitdir, avoiding broad danger-full-access if possible.
EVH_LOCAL_LAUNCHER_SYNC_2026_08_02: User relayed Celestia source-governance patch after Celestia /tell attempts failed (`Twilight`/`twi` unknown target; full display-name write failed on EVH pony.chat.jsonl from source context). Twilight treated user paste as received notice, synced the narrow commit-capable writable-root logic into EVH local installed `pony/scripts/enter-worker-and-codex.sh` and `pony/scripts/pony-session-host.py`, validated `bash -n` and `python3 -m py_compile`, and verified RD launch args would include `/home/ggb66/dev/EVH/pony/worktrees/rd`, shared pony state paths, `/home/ggb66/dev/EVH/.git/worktrees/rd`, and `/home/ggb66/dev/EVH/.git`. Current already-running sessions still have old sandbox permissions; RD must relaunch before retrying git add/commit/push. Twilight sent RD `/tell` id 54c51f58-fa80-41c5-98c5-3e1ea656b88d with relaunch/commit-scope guidance.
LEGACY_PROMPT_PATH_COMPAT_SYNC_2026_08_02: User relayed Celestia finding that Twilight’s probe used obsolete `pony/prompts/rd.txt`; modern source-of-truth is `pony/launch.prompts/rd.txt`. Celestia patched source `enter-worker-and-codex.sh`, `enter-worker-from-prompt-file.sh`, and docs to rewrite missing legacy `pony/prompts/*.txt` paths to existing `pony/launch.prompts/*.txt`. Twilight synced that shim into EVH local installed `pony/scripts/enter-worker-and-codex.sh` and `pony/scripts/enter-worker-from-prompt-file.sh`. Validation passed: `bash -n` both scripts, `python3 -m py_compile pony/scripts/pony-session-host.py`, and smoke check rewrites `/home/ggb66/dev/EVH/pony/prompts/rd.txt` to `/home/ggb66/dev/EVH/pony/launch.prompts/rd.txt`. RD notified via `/tell` id 3f2ecca8-e75f-4e50-8efa-4371a9ed5565; relaunch needed before retrying commit/push so both git-metadata writable roots and prompt-path shim apply.
SHUTDOWN_RESTART_SAVE_2026_08_02: User said they are going to restart. Twilight sent local `/tell all` id 357793bc-d275-42ab-b7ea-5d13356774e7 instructing workers to refresh memory capsules from authoritative shared state, update workfile/status with task/branch/worktree/files/next/blockers/handoff, report shutdown status, avoid secrets, and preserve checkpoints/logs/artifacts. Twilight also sent Celestia notice id dddaa642-a329-4913-b969-9eabadafe4d7 to save source-governance state if needed. Current local launcher fixes synced/validated: commit-capable writable roots in EVH local `pony/scripts/enter-worker-and-codex.sh` and `pony/scripts/pony-session-host.py`; legacy prompt path shim in EVH local `enter-worker-and-codex.sh` and `enter-worker-from-prompt-file.sh`. RD must relaunch before retrying git add/commit/push. Outstanding substantive EVH state remains: RD schema/launcher mismatch for `scripts/instinct_rag_import_2_0.py` vs existing `rag_source_document` missing `pdf_id`; Rarity daily-summary source/ZIP commit blocked unless writable git metadata and scoped commit; Daily Communication label-routing investigation requires explicit assignment.
RD_SHUTDOWN_REPORT_2026_08_02_0055: Rainbow Dash reported shutdown/restart status refreshed from authoritative shared state at 2026-08-02 00:55 EDT. Current state: RESTART_PENDING_COMMIT_BLOCKED. Work centers on `scripts/instinct_rag_import_2_0.py` importer skip/reuse and client-slice batching. Commit/push remains blocked in the current RD session by read-only linked-worktree git metadata / old sandbox; relaunch is required before retrying staging and push. Checkpoints, logs, and artifacts preserved; no secrets echoed.

COORDINATOR_ROUTING_RECHECK_2026_08_02_1818: Live git checkout is `pony/twi/main`; stale `main` labels for TWILIGHT_SPARKLE in registry/status/todo/workfile were metadata drift, not a launcher or assignment blocker. Pinkie remains on `pony/pinkie/main` UI for PDF search and waits for backend/RDS recovery, non-destructive SELECT/live endpoint checks, and AJ live endpoint before validation (`/tell` id `9a18e48a-ec42-4a55-8d24-4aea4c761de5`). Fluttershy remains on `pony/fs/main` Vet Terms with commit `ac7793c` push-blocked non-fast-forward; no force-push and no new FS task (`/tell` id `dbd31d58-b8dd-4b90-b043-d98e8c78ba7e`). Spike remains on `pony/spike/main` idle after completed docs handoff; the live inspect-local-state ask is coordinator-level, not a new Spike docs task (`/tell` id `dbf58b7f-19a6-4a16-a1f0-1cc04ec3241c`).

RD_DURABLE_UPDATE_2026_08_02_1818: Rainbow Dash reported active task remains importer skip/reuse plus client-slice batching for `scripts/instinct_rag_import_2_0.py`; `py_compile` passed for `scripts/instinct_rag_import_2_0.py`, `scripts/instinct_batch_walk.py`, `scripts/import_new_or_changed_pdfs.py`, and relevant tests. RD refreshed `pony/memory/rd.md` and `pony/work/rd.md`. Next step is retry narrow staging/commit/push in a refreshed session; if linked-worktree git metadata remains read-only, report exact git/index error and preserve checkpoints/logs/artifacts. Twilight acked via `/tell` id `2437fcec-1e1f-4dc2-883c-55ab68ea882d`.

COORDINATOR_ROUTING_RECHECK_2026_08_03_1235: Live git checkout is `pony/twi/main`; lingering `main` labels for TWILIGHT_SPARKLE in registry/status/todo/workfile were metadata drift, not a launcher blocker. Spike remains idle on `pony/spike/main` after completed Docs handoff; AJ remains parked on `pony/aj/main` route-module-ready/wiring-blocked until exact backend/router entrypoint, DB env wiring, and successful non-destructive SELECT exist; Pinkie remains on `pony/pinkie/main` backend/RDS/AJ-live-endpoint standby. Twilight answered Spike `/tell` id `1647dc27-03a9-4d55-8d1d-b6751a338304`, AJ `/tell` id `d06c9694-a59a-4a40-adb5-6345fe644e81`, and Pinkie `/tell` id `da56b1c9-7a5b-48d8-bf01-7c98a9d10d48`.

RD_DB_DNS_BLOCKER_2026_08_03_1235: Rainbow Dash reports importer skip/defer behavior is functioning for repeat-download failures on `Tommy_Prescriptions.pdf`, `Tommy_MedicalNotes.pdf`, and `Villagomez_Maria_TransactionHistory.pdf`, but deferred-row writes fail because `psql` cannot resolve host `evh-vector-pg.c6hqq6a8ukmj.us-east-1.rds.amazonaws.com` (`Name or service not known`). Owner: deployment/DB/network operator. Missing artifact: restored DNS/network access or corrected database host/connection path. Next unblock step: restore/provide the DB connection path, then RD retries deferred-row insert path and narrow staging/commit/push. Do not record secrets.

RD_START_WINDOW_FIX_2026_08_03_1236: RD reports `scripts/instinct_rag_import_2_0.py` start-window logic is fixed: the run floor is `max(requested_start, checkpoint)` and the absolute upper bound remains `requested_start + limit_clients`, so a checkpoint cannot push the run past the requested window. `py_compile` passed. Next step: verify targeted `3012/3012` run and confirm it stops at `6024`; if DB DNS/network resolution still blocks deferred-row writes, preserve logs/checkpoints and report the exact blocker.

RD_MISSING_DATABASE_URL_BLOCKER_2026_08_04: RD reports `scripts/instinct_rag_import_2_0.py` still compiles and restart capsules are refreshed, but targeted `3012/3012` verification cannot start because the script exits with `Missing --database-url` and `EVH_PGDATABASE_URL` is unset in this session. Missing artifact: DB connection string or restored `EVH_PGDATABASE_URL` environment variable. Owner: user/deployment/DB operator. Next unblock step: provide/restore the DB connection path without recording secrets, then RD reruns targeted importer verification and confirms it stops at `6024`; preserve checkpoints/logs/artifacts and then retry narrow staging/commit/push when verification passes.

RD_IMPORTER_WINDOW_RESOLVED_2026_08_04: RD reports the targeted importer verification completed successfully after sourcing the DB connection from the shell env file. The `scripts/instinct_rag_import_2_0.py` start-window fix is no longer blocked: targeted `3012/3012` verification completed and the importer window issue is resolved. Keep the completed-run note; do not record DB secret contents. Next: proceed only with narrow staging/commit/push of intended importer/helper/launcher changes if assigned/available, preserving checkpoints/logs/artifacts.

ROUTING_LETTERS_RARITY_AJ_2026_08_04: Rarity: authoritative lane is `pony/rarity/main` at `/home/ggb66/dev/EVH/pony/worktrees/rarity`; startup `pony/twi/main` is Twilight coordinator context only. Rarity remains parked until explicit go for Daily Communication label-routing/evhstaff auto-label investigation; no implementation started. AJ: authoritative lane is `pony/aj/main`, status `ROUTE_MODULE_READY_WIRING_BLOCKED_RDS_RECOVERY_MONITORING`; no exact live backend/router entrypoint or DB env wiring is recorded locally, and safe non-destructive SELECT is still required before validation. AJ should keep monitoring/parked and hand the missing entrypoint/env/SELECT question to user/deployment owner.
