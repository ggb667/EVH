# Twilight Workfile

Project: EVH
Branch: main

Status: HOLD_RARITY_DAILY_SUMMARY_ADP_FILTER_HANDOFF_RECORDED
Scope: coordinate assigned EVH workers
Permissions granted: none recorded
Restart capsule:
- task: none recorded
- why: none recorded
- next: none recorded
- blocker: none recorded
Notes:
- startup fast path: after dirty-worktree preflight, read `assignment.registry.tsv`, `*.status.md`, `twi.todo.md`, `twi.decisions.md`, `twi.mailbox.md`, and this workfile; treat `twi.event.stream.history.md` as a short rolling log and do not open `*.archive.md` files unless current state explicitly points there
- canonical worker scopes are assigned in pony/team.coordination/assignment.registry.tsv
- keep workers aligned to their current EVH areas and handle follow-up routing
- owned script directory: `scripts/coordination/`
- worker isolation rule: AJ owns `scripts/reminders/`, Pinkie owns `scripts/contacts/`, FS owns `scripts/schedule/`, Rarity owns `scripts/stockroom/`, RD owns `scripts/vetcove/`, Spike owns `scripts/docs/`, and Twilight owns `scripts/coordination/`
- branch rule: every worker stays on their own pony branch namespace and avoids shared root branches for implementation work; Twilight coordinates from the EVH root worktree on `pony/twi/main`
- current project state: reminder importer/doc work is now documented and pushed
- current repo state: Twilight coordinates from `/home/ggb66/dev/EVH` on `pony/twi/main`; keep root-worktree coordination changes there rather than on a worker branch
- 2026-07-10 routing note: Celestia reports source-runtime launcher governance patched in agenic-pony-system; no live EVH sessions were touched. Local Twilight branch fields should remain aligned to live `pony/twi/main`, not `main`.
- 2026-07-20 ponyalert loop note: Celestia fixed source watcher `pony/scripts/watch-twi.sh` in agenic-pony-system so unchanged pending Twilight request fields no longer replay `ponyalert` on every `.status.md` rewrite; validation passed via bash syntax and unit test. EVH local mute workaround was restored and should not be treated as root-cause fix.
- worker state: Spike was rebased onto `origin/main` and pushed as `655d7e5`
- current RAG coordination correction: AJ worker-side mailbox says the shared load target is Handshake's Aurora MySQL, MariaDB-compatible database, not Postgres/pgvector
- current RAG coordination correction: AJ reports the shared dictionary seed already loaded and verified at 3,133 `rag_dictionary_term` rows, so workers should not retry that seed/load path on relaunch
- 2026-07-12 RD import routing: user supplied Instinct Partner API credentials in chat for the partner environment and assigned RD's active slice as the live supervised PDF import. Do not persist or echo the password; route RD to use secure env/credential handling, preserve `/tmp/evh_instinct_import.checkpoint.json`, and continue run/analyze/fix/restart until every record is processed.

- 2026-07-14 Pinkie reassignment: user corrected Pinkie scope to UI for PDF search; Weave Contacts is no longer Pinkie's active assignment. Assignment registry and Pinkie shared state were updated accordingly.
- 2026-07-14 Pinkie progress letter: Pinkie took over UI for PDF search, updated `scripts/rag_ui/static/index.html` as EVH Instinct PDF RAG with a PDF query intent panel and preview state, refreshed her work/status, and reports `tests/test_rag_ui.py` passed. Next routing need is backend document-search/source-PDF results contract for final wiring; owner is Twilight until RD/AJ backend owner is assigned and the endpoint/response shape is handed to Pinkie.
- 2026-07-14 Rarity reassignment: user corrected Rarity scope to EVH Mail Archival and Organization; Meds & Treatments/Stockroom is no longer Rarity's active assignment unless explicitly resumed. Assignment registry and Rarity shared state were updated accordingly.
- 2026-07-14 Rarity progress letter: Rarity inspected active mail/archive surfaces and moved into documentation/inventory of current mailbox/archive materials plus the Handshake PDF explorer prototype. No blocker yet; next is completing the small inventory doc and refreshing Rarity state with the next archival slice.
- 2026-07-14 Rarity first archival slice complete: `docs/evh-mail-archive-inventory.md` exists, `docs/handshake-pdf-browser/README.md` links back to it, and Rarity next points at `scripts/coordination/archive/` for cleanup/rename/cross-link work. No blocker recorded.
- 2026-07-14 Spike evhstaff Google Mail check: Spike found Rarity's request in `evh_spike.mailbox.md` but no authoritative handoff, credential owner, or access-details record for an evhstaff Google Mail account in local EVH coordination state. Local docs mention `evhstaff@gmail.com` as sample/import data only, not as an authorization handoff. Safe routing: do not create a new mailbox yet; user/Twilight must confirm any existing authorized account and credential owner first.
- 2026-07-14 Rarity EVHStaff Gmail Cleanup blocker: evhstaff@gmail.com Gmail cleanup is blocked by Google verification error 403 access_denied: app is still in testing and only developer-approved testers may access it. Shared state mismatch corrected from blocker none/archive next-slice to blocked. Missing artifact/owner: user/Twilight must provide an existing authorized tester/account or app-verification/allowlist approval path; otherwise Rarity pauses EVH mail archival at launcher boundary.
- 2026-07-14 Spike Gmail archival durable update: working helper `scripts/gmail/evhstaff_gmail_inventory.py`; required scope `https://mail.google.com/`; reset/re-auth by deleting token cache and rerunning OAuth installed-app flow with `prompt=consent`; `gmail.modify` was insufficient for permanent deletion; observed result `deleted_count=3303`, `permanent_delete=true`. Command pattern: `python scripts/gmail/evhstaff_gmail_inventory.py --client-secrets /path/to/evhstaff_gmail_google_client_credentials.json --token-file /path/to/evhstaff_gmail_token.json --query 'older_than:3y' --export-zip /path/to/archive.zip --delete-after-export`. Do not persist real client-secret/token/archive paths in coordination state.
- 2026-07-15 RD routing correction: RD workfile had stale no-body size-probe/hold language, but shared state now records active live supervised Instinct PDF import. Current visible /tmp stop differs from older Blair_MedicalNotes.pdf report: checkpoint is updated 2026-07-14 21:34 EDT and fixed run exits 1 at `EXAMREPORTCARDCBC2.2.doc` invalid PDF/OLE header / PdfStreamError; `/tmp/evh_instinct_import.supervised.log` is not visible from this session. Route RD to preserve checkpoint, inspect current stop artifacts, fix non-PDF/corrupt-PDF handling, sanity-check edits, and restart from checkpoint rather than returning to the old size-probe blocker.
- 2026-07-15 RD restart update: RD inspected checkpoint/fixed output, patched `scripts/instinct_pdf_chunker.py` so non-PDF/corrupt-PDF extraction failures become DeferredOCRDocument rather than crashes, ran compileall + AST/tabnanny sanity, and restarted `/tmp/evh_supervise_import.sh` from checkpoint. Twilight local check saw supervised log present but empty and no supervisor/import process visible; next route is to watch `/tmp/evh_instinct_import.supervised.log` for progress past `EXAMREPORTCARDCBC2.2.doc` or inspect restart output if it stays empty.
- 2026-07-16 Rarity Gmail sender taxonomy update: Rarity reports `scripts/gmail/evhstaff_gmail_inventory.py` now uses the merged 15-category sender taxonomy, defaults OpenAI fallback to `gpt-5.6-luna`, and passes `reasoning.effort` plus `text.verbosity` in the OpenAI request payload; `python3 -m py_compile` passed. Next rerun should use `--openai-classifier-model gpt-5.6-luna --openai-reasoning-effort none --openai-text-verbosity low` and inspect `/tmp/evh_gmail_sender_routing_map.regen.json`.
- 2026-07-16 Rarity OpenAI fallback mini update: Rarity superseded the earlier `gpt-5.6-luna` rerun note by swapping the OpenAI fallback default in `scripts/gmail/evhstaff_gmail_inventory.py` to `gpt-5.6-mini`; `reasoning.effort=none` and `text.verbosity=low` request-payload threading remain intact, and `python3 -m py_compile` passed. Next rerun should use `--openai-classifier-model gpt-5.6-mini --openai-reasoning-effort none --openai-text-verbosity low` and inspect `/tmp/evh_gmail_sender_routing_map.regen.json`; reserve Terra for a later leftovers pass if needed.
- 2026-07-16 dirty-worktree preflight: inspected pending local changes, confirmed the live branch is `pony/twi/main`, restored Twilight branch metadata in shared state, reconciled Rarity's current Gmail state to no routing blocker with mini rerun/inspection as the next step, and put away the large local zip artifact outside the repo.
- 2026-07-16 RD import restart update: RD and Spike confirmed the live import moved past `EXAMREPORTCARDCBC2.2.doc` and shifted to deferred-OCR / psql write-path failures; RD patched `scripts/instinct_full_import_fixed.py` so deferred-OCR DB writes are best-effort on psql disconnects, ran syntax/tabnanny sanity, and relaunched from `/tmp/evh_instinct_import.checkpoint.json` with sourced Postgres + Instinct env. Twilight local check at 12:16 EDT saw `/tmp/evh_instinct_import_fixed.out` and checkpoint still updating, empty status/exitcode files, and no visible import process from this session; next watch is `/tmp/evh_instinct_import_fixed.out`, `/tmp/evh_instinct_import_fixed.status`, and `/tmp/evh_instinct_import_fixed.exitcode`; blocker only if vector DB disconnects persist or a new fatal error appears.
- 2026-07-16 Rarity Gmail sender-routing picker/rebuild update: Rarity completed `scripts/gmail/review_sender_routing_picker.py` with Gmail preview fetching/caching/fallback queries, Escape/q save-and-exit, backup-on-same-input-output handling, `Uncategorized` no-fetch fallback, and compile verification. Generated `NamesEmailAddressesCategoriesSource.txt` / `reviewed_email_categories.txt` are contaminated by mixed sender data from a non-evhstaff mailbox export; correct recovery is to restart tomorrow from the single mailbox `evhstaff@gmail.com` only, rebuild the routing list from scratch, and not reuse the mixed source list.
- 2026-07-17 shutdown state saved: Twilight is on `pony/twi/main` in `/home/ggb66/dev/EVH`; no pending user approvals or Twilight decisions. Resume by watching RD fixed importer artifacts and preserving checkpoint, then route Rarity to keep the picker script but rebuild Gmail sender routing from the single mailbox `evhstaff@gmail.com` only without reusing contaminated mixed-source artifacts.

- 2026-07-17 coordinator-routing correction after worker letters: live branch verified as `pony/twi/main`; local registry/status/workfile stale `main` metadata corrected. Pinkie is active on PDF-search UI inspection, RD active on fixed-importer timing relaunch, Spike on Docs upkeep for RD totals/pagination, Rarity on Gmail sender-routing rebuild from `evhstaff@gmail.com` only with zero-byte cache regenerated/omitted, and FS on vet-term taxonomy maintenance.

- 2026-07-17 Rarity issue refined after RD push contrast: RD pushing does not disprove Rarity blocker. This Twilight context cannot create Git index locks anywhere under `.git`. Rarity target files are currently in root EVH worktree (`scripts/gmail/`, docs) on `pony/twi/main`, while Rarity worktree lacks `scripts/gmail/` and shows older stockroom changes. Need choose writable commit lane: root/Twilight context for root-visible files, or apply/copy to Rarity worktree and stage from a write-capable Rarity shell.

- 2026-07-18 shutdown order sent: `/tell all` id `3c774b78-3da1-43da-97b4-c9c1acd5f7fc` instructed workers to commit/push if possible, save memory capsules/workfiles/status, record exact blockers, and report shutdown status. Known restart blocker remains Rarity writable commit lane/git metadata for Gmail/docs changes; daily-summary send already succeeded once, no further real sends approved.

- 2026-07-18 shutdown reports partial: user clarified commit scope = project deliverables/code/docs/data only, not pony metadata. Rarity reports project commits `a49abea` and `8a96689` pushed, no blocker. RD reports project commit `b9f90cf` pushed, metadata local only, no blocker. Spike reports no project deliverables changed; metadata local only; Spike shell git metadata read-only but no commit needed; `.codex` scratch remains uncommitted. Await optional AJ/Pinkie/FS reports if session remains open.

- 2026-07-18 AJ shutdown report: AJ refreshed shutdown metadata locally and prepared DB deliverables, but commit/push is blocked because `/home/ggb66/dev/EVH/.git/worktrees/aj/index.lock` cannot be created on read-only filesystem. Stage only project deliverables from a writable context later; do not commit pony metadata.

- 2026-07-18 shutdown reports complete: all workers reported. Rarity pushed `a49abea`/`8a96689`; RD pushed `b9f90cf`; Pinkie pushed `9deae36`; Spike had no deliverables to commit; AJ prepared DB deliverables but commit/push blocked by read-only git metadata; FS has local commit `ac7793c` but push blocked non-fast-forward. Pony metadata local only per user clarification.

- 2026-07-18 dirty preflight/Pinkie recheck: inspected local dirty worktree. Untracked project deliverables (`docs/handshake-pdf-browser/README.md`, `scripts/gmail/clean_sender_routing_map_stdout.py`, `scripts/gmail/review_sender_routing_map.py`, `scripts/gmail/review_sender_routing_picker.py`) compile/reference cleanly enough to preserve, but staging them from this Twilight context is blocked by read-only `/home/ggb66/dev/EVH/.git/index.lock`. Routine pony metadata remains local-only. Pinkie rechecked PDF-search UI: commit `9deae36` is pushed; final live renderer wiring remains blocked on backend document-search/source-PDF response shape (route, request params, hit fields, source-PDF/page URL fields, snippets, pagination), or a concrete UI-only reassignment.

- 2026-07-18 AJ/RD document-search contract ownership: AJ reports no implemented document-search route, but proposes `Proposed only (no implemented endpoint): GET /api/rag/documents/search; params: client_id required, pet_id optional, q optional (>=3 chars for text search), page 1-based default 1, page_size default 20 max 100, cursor opaque alternative to page, sort relevance|date_desc (default relevance when q else date_desc). Response items: document_id, source_system, source_reference_id, clinic_id, client_id, pet_id, filename, mime_type, page_count, ingest_status, summary, score, pages [{page_id,page_number,page_label,source_page_url,snippet,match_score,chunks [{chunk_id,chunk_index,snippet,score}]}], pagination {page,page_size,total,has_next,next_cursor}.` RD confirms `RD canonical PDF/source_reference_id is Instinct GraphQL ChartFile.id, persisted by importer as pdf_id; source_uri is the signed URL returned by createChartFileUrl(id=ChartFile.id, inline=true). RD has no native per-page identifier/page-link builder; extracted page_number is 1-based; pms_document_page.id must be backend-generated. Direct page link may be source_uri + #page=<page_number>, but signed URLs expire, so a stable backend/proxy page route is preferred.` Ownership: AJ validates route/DB projection and backend-owner acceptance; RD owns ChartFile/pdf_id/source_uri/page-number constraints; Pinkie waits for final accepted/implemented route and stable `source_page_url` semantics. Root untracked deliverables remain blocked by read-only `.git/index.lock`.

- 2026-07-18 final document-search validation plus Pinkie adapter: AJ/RD finalized id/link constraints: source_reference_id must be exact Instinct GraphQL ChartFile.id/importer pdf_id; never filename, content hash, or source_name. source_uri stores the observed createChartFileUrl(id=ChartFile.id, inline=true) signed URL for provenance only; it expires and is not canonical/stable. pms_document_page.id/page_id is backend-generated. pms_document_page.source_page_link/source_page_url is canonical for UI only when backend generates a stable proxy/link from source_reference_id + 1-based page_number; source_uri#page=N is temporary fallback only. AJ has no more contract changes pending backend-owner acceptance; `GET /api/rag/documents/search` remains proposed, not implemented. Pinkie incorporated the proposed adapter into `scripts/rag_ui/static/index.html` and reports `tests/test_rag_ui.py` passes (3 tests), but she is not treating route as live. Next backend need: owner/implementation for route and stable proxy source_page_url; next git need: writable lanes for root untracked deliverables and Pinkie adapter commit.

- 2026-07-18 Rarity daily summary formatter fix: Rarity fixed root `scripts/gmail/daily_email_summary.py`: removed rendered Query diagnostic, skipped empty category headings, changed sender emails from angle brackets to parentheses, and kept indented summaries inside the same HTML `li` with no extra paragraph spacing. Reported `py_compile` and focused formatter assertions pass. File is in Twilight root worktree; git metadata remains read-only for staging/commit.

- 2026-07-18 Rarity root worktree clarification: Clarification from Rarity: the worker worktree `scripts/` has no `scripts/gmail/daily_email_summary.py`; the formatter fix is in the project root/Twilight worktree at `/home/ggb66/dev/EVH/scripts/gmail/daily_email_summary.py`, which is the file the user identified. Rarity worker branch remains `pony/rarity/main`; commit lane needed is writable root git metadata for the root file.

- 2026-07-18 Rarity Lambda zip updated: Rarity updated `/tmp/evh-lambda.zip` after first creating backup `/tmp/evh-lambda.backup-20260718.zip`; replaced `scripts/gmail/daily_email_summary.py` in the archive with the fixed root version. Reported `unzip -tq` passed, extracted file SHA256 matches root, and `py_compile` passed. Twilight local check saw both zip files present and root file SHA256 `a5346e94c97371a42422d1c8d4f655d850170874e33f8e633c29ff300f53c910`.

- 2026-07-18 Rarity mailto fix Lambda zip updated: Rarity corrected the remaining `.vet` address issue in root `/home/ggb66/dev/EVH/scripts/gmail/daily_email_summary.py`: `inline_markdown_to_html` now manually `mailto:`-links every email address, including `vetinfo@animalbiome.vet`, while preserving visible parentheses. Reported root `py_compile` and focused HTML checks pass. Updated `/tmp/evh-lambda.zip` after backup `/tmp/evh-lambda.before-mailto-20260718.zip`; reported unzip integrity, extracted/source SHA256, and ZIP mailto check pass. Twilight local check saw `/tmp/evh-lambda.zip` updated at 10:16, backup present, and root SHA256 `5d0f3cb4ae68f161cfc7c884c80400210a067e379a9c6af8ada1ff9b4601129b`.

- 2026-07-18 deploy Lambda zip ready for version control: User requested the Lambda ZIP be version-controlled instead of living only in `/tmp`. Rarity copied the current validated archive to `/home/ggb66/dev/EVH/deploy/evh-lambda.zip`. It is untracked (`?? deploy/evh-lambda.zip`); reported `unzip -tq` passed and SHA256 matches `/tmp/evh-lambda.zip` as `3fac4d63809f61fe1d220c41a9a9fa87058531f1023e0af790b5ebe0b7d09e6e`. Twilight local check confirmed file sizes, matching SHA256, unzip integrity, and untracked status. Stage/commit from writable root git metadata.

- 2026-07-18 deploy Lambda zip pushed correction: Follow-up correction: Twilight local check found `/home/ggb66/dev/EVH/deploy/evh-lambda.zip` is already tracked in commit `b90c806` (`Add current EVH Lambda deployment bundle`) on `pony/twi/main`; Twilight pushed `b90c806` to `origin/pony/twi/main`. Treat the Lambda ZIP version-control request as fulfilled. Remaining root dirty blocker still includes modified `/home/ggb66/dev/EVH/scripts/gmail/daily_email_summary.py` and other untracked root deliverables, but not `deploy/evh-lambda.zip`.

- 2026-07-18 Rarity daily summary 1:1 source+zip update: Rarity implemented the requested 1:1 daily-summary behavior in root `/home/ggb66/dev/EVH/scripts/gmail/daily_email_summary.py` and rebuilt `/home/ggb66/dev/EVH/deploy/evh-lambda.zip`: prompt now requires every supplied message; `reconcile_summary_result` fills omitted/duplicate model rows into Other and recalculates counts; unread sections render first, follow-up notes next, read sections afterward; sent replies are matched by recipient/normalized subject and rendered with Gmail link plus one-line body summary. Reported source `py_compile`, focused reconciliation test, ZIP integrity, and Lambda wiring checks pass. Twilight verified `py_compile`, deploy ZIP integrity, source SHA256 `8f65f0df2971ce0ac7a32b0eb8b23c74c16ae4e409cfd2d26f48d18723e18416`, deploy ZIP SHA256 `fea401b34f5a95af093fb2de5bbbd428f7a62d81c80ebab6a1854f1fa882401f`, and ZIP-contained `scripts/gmail/daily_email_summary.py` matches source. Staging/commit/push of only the source+ZIP is blocked by `/home/ggb66/dev/EVH/.git/index.lock` read-only; do not stage unrelated dirty files.

- 2026-07-18 Rarity daily summary 1:1 commit retry failed: Rarity retried commit/push for the current daily-summary 1:1 fix, but `git add scripts/gmail/daily_email_summary.py deploy/evh-lambda.zip` failed because `/home/ggb66/dev/EVH/.git/index.lock` cannot be created (`Read-only file system`). No commit or push occurred; both files remain unstaged. Next action: run from a writable root git-metadata context, staging only `scripts/gmail/daily_email_summary.py` and `deploy/evh-lambda.zip` and no unrelated dirty files. Twilight local status confirms both target files are still modified/unstaged.

- 2026-07-18 ggb667 alternate Lambda zip created: Rarity created alternate artifact `/home/ggb66/dev/EVH/deploy/evh-lambda-ggb667.zip` for summarizing `ggb667@gmail.com`. It adds `GMAIL_EXPECTED_MAILBOX_EMAIL` configuration, passes expected mailbox into verification, and uses that mailbox as sender; current default behavior remains `evhstaff@gmail.com`. Deployment must use an OAuth secret containing `ggb667@gmail.com` refresh credentials and set `GMAIL_EXPECTED_MAILBOX_EMAIL=ggb667@gmail.com`; set `DAILY_SUMMARY_TO` separately if desired. Reported ZIP integrity passed. Twilight local check confirmed file exists, is untracked, `unzip -tq` passes, and SHA256 is `cfbae9cadb760ebd142957c66268d383da18cf4438900656ecd3e625eba4db95`. Do not persist OAuth secrets in coordination state.

- 2026-07-18 ggb667 OAuth secret blocker: Local EVH state contains no confirmed OAuth secret/ARN or credential handoff for `ggb667@gmail.com`. The alternate ZIP `/home/ggb66/dev/EVH/deploy/evh-lambda-ggb667.zip` cannot run against that mailbox until the owner provides an AWS Secrets Manager secret ARN whose refresh token belongs to `ggb667@gmail.com` plus `client_id`/`client_secret`, and the Lambda environment sets `GMAIL_EXPECTED_MAILBOX_EMAIL=ggb667@gmail.com`. Do not put secret contents in coordination files. Owner: user/deployment operator. Next unblock step: provide/record only the secret ARN and deployment env setting, not secret contents.

- 2026-07-18 RD OCR speed review: RD OCR speed review: biggest current costs are Ghostscript rasterizing every page at 200 DPI color PNG (`png16m`), one serial Tesseract subprocess per page (`--psm 6`), temporary disk I/O, reprocessor processing strictly one PDF at a time, and opening `psql` for each status update. Safe speedup plan: benchmark lower DPI/grayscale or bilevel output first; add bounded page-level parallelism without oversubscription; batch status updates or keep one DB connection; add per-stage timing; preserve fallback quality. Signed `source_uri` expiry is a separate operational risk. No code changed yet.

- 2026-07-18 RD import DB auth/pg_hba blocker: RD importer behavior/blocker: current importer is doing per-record continuation, not true recovery. `Charlie_MedicalNotes.pdf` failed during `load_into_postgres` with persistent DB auth/pg_hba rejection, then importer marked it failed and advanced to `Charlie_Prescriptions.pdf`. It will likely repeat for every record and waste runtime until EVH Postgres credentials/allowlist/SSL are fixed. Error shows password authentication failure plus no `pg_hba` entry for source host `104.136.227.36`, database `evhvector`, user `evhadmin`. Pause/restart only after DB owner fixes credentials and pg_hba/network/SSL settings; preserve `/tmp/evh_instinct_import.checkpoint.json`. Owner: AJ/DB owner plus user/deployment operator for secret/network changes. Missing artifacts: valid EVH Postgres credential set, pg_hba/network allowlist for source host `104.136.227.36`, and SSL setting compatible with the connection. Routed live messages to RD and AJ.

- 2026-07-18 AJ acknowledged RD import DB blocker: AJ acknowledged routed DB blocker and refreshed restart state: do not restart importer until `evhadmin` credentials, `pg_hba` for source host `104.136.227.36`, network reachability, and SSL settings are corrected for database `evhvector`; RD checkpoint must remain preserved. Document-search contract remains unchanged. Live ack to AJ and RD sent.

- 2026-07-18 RD/AJ DB diagnosis narrowed to password after SSL: RD narrowed DB diagnosis: after sourcing local helpers, SSL-required `psql` reaches RDS and returns only `password authentication failed for user evhadmin`; egress IP `104.136.227.36` is confirmed. Earlier no-`pg_hba`/no-encryption error was the non-SSL path. RD patched `scripts/run_instinct_import_fixed.py` fallback DB URL to append `?sslmode=require`; reported `py_compile`, AST, and tabnanny pass. Twilight locally verified `py_compile`, AST, and tabnanny on `pony/worktrees/rd/scripts/run_instinct_import_fixed.py`. Remaining external fix is reset/provide the correct `EVH_PGPASSWORD` or `EVH_PGDATABASE_URL` credentials for `evhadmin`; RD cannot change the RDS role password. Preserve checkpoint and do not restart importer until credential fix is confirmed. AJ tested the configured EVH Postgres connection without exposing secrets: TCP reachability succeeds, `pg_isready` reports accepting connections, and the SSL-capable server responds. A non-destructive SELECT fails specifically at password authentication for `evhadmin`; network is not the current blocker. Route deployment/DB owner for a valid rotated `evhadmin` credential and pg_hba verification/allowlist for `104.136.227.36`; after that AJ can re-test SELECT, but do not restart RD importer before success. RD checkpoint remains preserved. Live messages sent to RD and AJ.

- 2026-07-18 CloudShell network path differs from local helper: AJ CloudShell test differs from local helper: `psql` with `sslmode=require -w` timed out after 15s (exit 124), so CloudShell currently has no network path to RDS and this is not an authentication result. Deployment/DB owner must check RDS `Publicly accessible`, subnet route/NACL, and security group TCP 5432 from the CloudShell egress IP; do not open `0.0.0.0/0`. If DB is private, use VPC-connected CloudShell or an EC2 bastion. Keep RD importer paused and preserve checkpoint. This does not replace the local-helper finding that local SSL path reached RDS and then failed password auth; it adds a separate CloudShell network-path blocker. Live messages sent to AJ and RD.

- 2026-07-18 RDS Internet access gateway disabled: AJ found the AWS RDS Connect page shows Internet access gateway: Disabled. That likely explains CloudShell `psql` timeout 124: CloudShell has no path to this RDS endpoint, so credentials remain untested from CloudShell until a network path exists. Recommended private fix is VPC-connected CloudShell or EC2/SSM bastion in the RDS VPC. Only temporarily enable public access if deployment owner approves, with security group TCP 5432 restricted to the actual client `/32`, never `0.0.0.0/0`. Keep RD importer paused and preserve checkpoint. Owner: deployment/DB owner. Live messages sent to AJ and RD.

- 2026-07-18 DB blocker cleared RD resume confirmed: AJ reports DB blocker cleared: after allowing temporary CloudShell egress `3.238.126.34/32` on security group `sg-0d4114e2b4ab03378` and downloading the RDS global CA bundle, CloudShell `psql` with `sslmode=verify-full` succeeded (`current_database=evhvector`, `current_user=evhadmin`, `EXIT=0`). Credentials are valid and the password was not persisted. Twilight confirmed RD may resume from preserved checkpoint using the SSL-required path. Cleanup required: deployment/DB owner must remove the temporary SG rule when import/testing is done. Live messages sent to AJ and RD.

- 2026-07-18 RD restart plus AJ local credential verification caution: RD reports authorized restart completed: fixed importer resumed from `/tmp/evh_instinct_import.checkpoint.json` with sourced local env and SSL-required launcher. Initial checkpoint now reports 7,927 `processed_pdf_ids`, current `pdf_id` 66207 (`Blaze_MedicalNotes.pdf`), `last_error` null; `/tmp/evh_instinct_import_fixed.out` is advancing through `chunk_start`. RD will continue monitoring. Temporary SG rule `3.238.126.34/32` on `sg-0d4114e2b4ab03378` remains for deployment/DB-owner cleanup after completion. AJ clarified local importer environment: public egress is `104.136.227.36`, which already exists in RDS SG `sg-0d4114e2b4ab03378`; local network allowlist is covered. CloudShell `3.238.126.34/32` rule was only for the diagnostic. Remaining local action is update `EVH_PGPASSWORD` or `EVH_PGDATABASE_URL` to the working Secrets Manager credential with SSL, run a local SELECT, then RD may resume after confirmation. Because RD already reported a restart, Twilight routed a verification caution: RD should immediately verify the running environment has the corrected credential and report first successful DB write/SELECT; pause if password auth failures recur. Live messages sent to RD and AJ.

- 2026-07-18 stale local credential restart stopped: AJ inspected `/home/ggb66/dev/postgress_connection.zsh` with values redacted: it exports `EVH_PGPASSWORD`, `EVH_PGHOST`, `EVH_PGPORT`, `EVH_PGDATABASE`, and `EVH_PGUSER`; it does not define `EVH_PGDATABASE_URL`. The password is duplicated in the file. This is the local credential source, but its password has not yet been proven to match Secrets Manager. Do not record secret contents. RD immediate check found restart used stale local helper credentials: `EVH_PGPASSWORD` length 28, `EVH_PGDATABASE_URL` unset, and `/tmp/evh_instinct_import_fixed.out` contains repeated password-auth/no-encryption failures during `embed_load`. No successful DB SELECT/write occurred. RD stopped importer with Ctrl-C immediately; checkpoint is preserved with `current_pdf_id` 93473, `processed_pdf_ids` count 7207, `last_error` null. Do not restart until deployment updates `/home/ggb66/dev/postgress_connection.zsh` or `EVH_PGDATABASE_URL` to the working Secrets Manager credential, then run SSL `verify-full` SELECT first. Live messages sent to AJ and RD.

- 2026-07-18 performance launcher stale credential SSL patch needed: RD reports a performance launcher was manually started despite the pause: `scripts/run_instinct_import_rag_perforamnce_fixed.py` (visible in RD worktree as `pony/worktrees/rd/scripts/run_instinct_import_rag_perforamnce_fixed.py`). It does not include the SSL-required URL patch and sources the stale local helper credentials. Ctrl-C interrupted the launcher wait; no importer process is currently running. The run advanced checkpoint to `current_pdf_id` 70783 with 7,943 `processed_pdf_ids` and `last_error` null; log ends at `chunk_start`, so DB success is not established. Keep paused. Required before any restart: deployment/user updates local helper or `EVH_PGDATABASE_URL` to the working Secrets Manager credential, local SSL `verify-full` SELECT succeeds, and this performance launcher is patched to `sslmode=require`. Live messages sent to RD and AJ.

- 2026-07-18 AJ refreshed importer blocker state: AJ refreshed blocker state: no importer process is running; checkpoint `current_pdf_id` 70783, 7,943 `processed_pdf_ids`, `last_error` null; log ends at `chunk_start` with no DB success. Keep paused. Required before restart: update stale `/home/ggb66/dev/postgress_connection.zsh` or provide `EVH_PGDATABASE_URL`, patch `scripts/run_instinct_import_rag_perforamnce_fixed.py` to enforce `sslmode=require`, then pass a local SSL `verify-full` SELECT. Live messages sent to AJ and RD.

- 2026-07-18 RD performance restart gate cleared: RD reports credential verification now succeeds locally: `psql` with `PGSSLMODE=verify-full` and the RDS global CA bundle returned `SELECT 1` with exit 0. RD patched `scripts/run_instinct_import_rag_perforamnce_fixed.py` fallback DB URL to append `?sslmode=require`; reported `py_compile`, AST, and tabnanny pass. Twilight locally verified `py_compile`, AST parse, tabnanny, and diff for `pony/worktrees/rd/scripts/run_instinct_import_rag_perforamnce_fixed.py`. The requested performance launcher is now the correct launcher path, but importer has not been restarted yet. Restart gate is cleared: RD may restart the performance launcher from preserved checkpoint and report first DB write/progress/failures. Cleanup reminder remains: deployment/DB owner must remove temporary CloudShell SG rule `3.238.126.34/32` on `sg-0d4114e2b4ab03378` when import/testing is done. Live messages sent to RD and AJ.

- 2026-07-18 RD Instinct invalid_token blocker: RD reports a new importer failure separate from Postgres: Instinct GraphQL patient-history fetch returned HTTP 403 `invalid_token` for `patient_id` 5563 after 3 attempts. Importer cannot discover charts until the Instinct API token in local credentials is refreshed/verified; DB credential/SSL success does not fix this. Keep importer paused, preserve checkpoint, refresh the Instinct API token, then run a small patient-history/API smoke test before restart. Missing external artifact: valid Instinct API token/local credential refresh. Owner: user/deployment operator or credential owner; RD can smoke-test after refresh. Do not record token contents. Live messages sent to RD and AJ.

- 2026-07-18 RD token recovery restart authorized: RD token recovery succeeded: refreshed Instinct token without echoing it, and direct GraphQL patient-history smoke test for `patient_id` 5563 returned HTTP 200 with 9 charts and no errors. RD patched `scripts/instinct_full_import_fixed.py` to copy the freshly authenticated adapter token into process `TOKEN`, including the reauth path, preventing stale-token reuse. Reported `py_compile`, AST, and tabnanny pass. Twilight locally verified `py_compile`, AST parse, tabnanny, and diff on `pony/worktrees/rd/scripts/instinct_full_import_fixed.py`. Importer was stopped pending restart decision; Twilight authorized restart of the corrected performance launcher from the preserved checkpoint using the SSL-required DB path, with checkpoint preservation and no token echoing. Report first patient-history fetch, first successful DB write, progress, or any new failure. Live messages sent to RD and AJ.

- 2026-07-18 RD fail-fast infrastructure policy ready: RD implemented fail-fast infrastructure policy plus bounded retries. Patient-history fetch retries 3 times, then raises fatal on `invalid_token`/auth/connection/SSL/dependency infrastructure errors. Embed/load retries infrastructure failures 3 times with 5s delay, then stops instead of skipping records. Ordinary document/PDF errors retain existing per-record handling. Performance launcher now tees child output to both screen and `/tmp/evh_instinct_import_fixed.out`, uses unbuffered child output, and terminates child on Ctrl-C. Reported `py_compile`, AST, and tabnanny pass. Twilight locally verified `py_compile`, AST parse, and tabnanny on `pony/worktrees/rd/scripts/instinct_full_import_fixed.py` and `pony/worktrees/rd/scripts/run_instinct_import_rag_perforamnce_fixed.py`; RD worktree diff shows those two files changed. No restart currently. Next restart should be an explicit decision after preserving checkpoint and confirming desired launch path. Live messages sent to RD and AJ.

- 2026-07-18 RD restart readiness rechecked: RD restart readiness rechecked: no importer process running; current preserved checkpoint is `current_pdf_id` 104760, `processed_pdf_ids` 7947, `last_error` null. Local DB SSL `verify-full` `SELECT 1` succeeded with `/tmp/rds-global-bundle.pem`. Fresh Instinct token acquired silently; patient 5563 GraphQL smoke returned HTTP 200, no errors, 9 charts. Corrected performance launcher/code checks remain passing. RD is ready to restart on explicit go; temporary SG cleanup reminder remains. Do not echo token contents. Live messages sent to RD and AJ; pending user explicit go.

- 2026-07-18 user manually launched RD performance importer: User reported they manually launched the corrected performance importer after RD's readiness recheck. Twilight routed RD to monitor the run from the preserved checkpoint, confirm first patient-history fetch and first successful DB write, report progress or any new failure, and keep checkpoint preserved. AJ was told to stand by for DB/SELECT/write follow-up. Temporary CloudShell SG cleanup reminder remains for deployment/DB owner after import/testing.

- 2026-07-18 RD import active with DB write confirmed: RD monitoring confirms manual restart is active: status/exitcode remain empty while log/checkpoint advance. First successful patient-history fetch in current log: patient 5175, 6 ChartFiles, 0.466s. First successful DB write: `Buffy_Prescriptions.pdf`, pdf_id 125137, patient 5175, 17 chunks/2 pages, `load_complete`. Current checkpoint: client `b28255a0-8b31-43db-a1db-12c8bd180d07`, patient 5178, current pdf 94458 `Spitzie_VaccineHistory.pdf`, pdf_count 23139, loaded_count 119, skipped_count 4410, last_error null. No fatal infrastructure failure observed; occasional patient-history retries are present. Temporary SG cleanup remains outstanding after import/testing. Live messages sent to RD and AJ.

- 2026-07-18 RD per-patient document progress update: RD added per-patient document progress to `scripts/instinct_full_import_fixed.py`: screen lines now include `[document N/M]`, where M is the patient ChartFile count (examples 1/11, 2/11). Existing global file/client counters remain. Reported syntax/AST/tabnanny pass; Twilight locally verified `py_compile`, AST parse, and tabnanny on `pony/worktrees/rd/scripts/instinct_full_import_fixed.py`. This takes effect on the next launcher restart; current run is not interrupted. Live ack sent to RD.

- 2026-07-18 RD deferred-PDF guard follow-up: RD investigation confirms repeated deferred-PDF concern: `pdf_id` 8122 (`McKinney_Terry_TransactionHistory.pdf`) appears in 5 process/defer cycles in the accumulated log, each due to a 45s extraction timeout; no `already_processed` event was logged for it. It is now present in checkpoint `processed_pdf_ids`, so future resumes should skip it, but prior repeated launches/checkpoints caused repeated giant downloads. Follow-up improvement: add a durable pre-download deferred/processed lookup or local manifest guard so known deferred PDFs are skipped before download. Current run should not be interrupted unless needed. Live ack sent to RD.

- 2026-07-18 RD durable skipped-PDF guard implemented: RD implemented durable skipped-PDF guard: importer now maintains `/tmp/evh_instinct_import.checkpoint.json.skipped.json`, checks skipped IDs before downloading, and records deferred/no-text/problem PDFs plus retry-skipped IDs immediately. A deferred giant PDF should now be downloaded/OCR-deferred once per checkpoint/manifest, then skipped on future runs. OCR reprocessor still consumes only pending deferred rows and marks each complete/failed. Existing processed IDs remain respected. Reported `py_compile`, AST, and tabnanny pass; Twilight locally verified `py_compile`, AST parse, and tabnanny on `pony/worktrees/rd/scripts/instinct_full_import_fixed.py`. Current run was not interrupted; change takes effect on next relevant code path/restart. Live ack sent to RD.

- 2026-07-18 RD repeated-work skip scan: Twilight scanned `/tmp/evh_instinct_import_fixed.out` for other repeated work to skip. Findings: (1) repeated deferred/problem PDFs not currently in `/tmp/evh_instinct_import.checkpoint.json.skipped.json`: `3153`, `3195`, `8122`, `76241`, `76244`, `76248`, `101429`; all are present in checkpoint `processed_pdf_ids`, so current future resumes should skip via checkpoint, but the skipped manifest is incomplete and could be seeded at a safe stop. Current skipped manifest only has `101418`, `101431`, `3174`, `3216`. (2) repeated successful `load_complete` for same pdf_ids from restart/checkpoint churn, including `101428`, `101426`, `101422`, `101424`, `99966`-`99970`, `3220`, `3178`, `131533`, plus smaller repeats like `110725`, `110728`, `3166`; consider a DB pre-download/pre-embed lookup by canonical `source_reference_id`/pdf_id with statuses like `load_complete`, `deferred`, `no_text`, or `failed_deferred` before signed URL/download. (3) repeated patient-history fetches for same patients after restarts (notably `5217`-`5223`); since signed URLs expire, do not cache URLs, but a small patient ChartFile id/name metadata cache could avoid repeated GraphQL discovery on rollback/restart. (4) old repeated invalid-token and password-auth failures are already addressed by fail-fast/token/DB changes. Recommendation: do not interrupt current run; add manifest seeding/DB preflight/patient chart-list cache at a safe stop or next restart patch. Live findings sent to RD.

- 2026-07-18 in-run duplicate document guard clarified: User clarified repeated-work guard intent: desired fix is an in-run hash table/status map keyed by stable document identity, preferably `(client_id, patient_id, pdf_id)`, checked before signed URL/download. If the same key appears again in the same live run, importer should skip immediately with an `already_seen_this_run` log/status. This is not primarily a real DB lookup and not manual skipped-manifest seeding; DB preflight can remain a later durable enhancement, while the in-run guard is lighter and prevents repeated work during one process. `pdf_id` alone may be globally unique, but the tuple is preferred for auditability and source-context safety. Routed to RD; AJ notified.

- 2026-07-18 duplicate guard controlled restart authorized: User approved applying the in-run duplicate document guard now. Twilight routed RD to perform a controlled safe restart so the patch takes effect: stop current launcher/importer cleanly, preserve checkpoint and skipped manifest, verify no importer process remains, restart the corrected performance launcher, then report restart checkpoint, first patient-history fetch, first DB write/progress, and any `already_seen_this_run` skips. Do not echo tokens. Temporary CloudShell SG cleanup reminder remains active. AJ notified to stand by for DB/progress follow-up.

- 2026-07-18 RD client-average ETA rework: RD reworked ETA per user request for next restart: `scripts/instinct_full_import_fixed.py` now times completed clients, computes average seconds/client, projects average across `expected_clients`, and subtracts elapsed runtime for whole-process ETA. It no longer uses PDFs-per-client rolling projection. ETA remains unknown until at least one client completes, then stabilizes as client samples accumulate. Current run was not interrupted. Reported `py_compile`, AST, and tabnanny pass; Twilight locally verified `py_compile`, AST parse, and tabnanny on `pony/worktrees/rd/scripts/instinct_full_import_fixed.py`, and diff shows the ETA projection now uses `completed_client_durations` / `average_client_seconds` instead of observed PDFs per client. Live ack sent to RD.

- 2026-07-18 RD remaining-client ETA correction: RD corrected the next-restart ETA formula: the first completed-client-duration implementation multiplied average seconds/client by all 12,053 `expected_clients`, overestimating when resuming from an existing checkpoint with about 2,050 clients already seen. Patched formula now multiplies by remaining clients only: `expected_clients - base_client_seen - completed_current_run_clients`, with the active client included in the remaining estimate. Current run untouched. Reported checks pass; Twilight locally verified `py_compile`, AST parse, and tabnanny on `pony/worktrees/rd/scripts/instinct_full_import_fixed.py`; diff shows ETA uses `completed_client_durations`, `completed_client_count = base_client_seen + len(completed_client_durations)`, and `remaining_client_count = max(expected_clients - completed_client_count, 0)`. Live ack sent to RD.

- 2026-07-19 RD permanent PDF retention implementation: RD implemented permanent PDF retention in `scripts/instinct_full_import_fixed.py`: new `--pdf-storage-dir` / `EVH_PDF_STORAGE_DIR`, default project-root `/home/ggb66/dev/EVH/data/instinct-pdfs`; stable sanitized `<pdf_id>.pdf`; atomic `.part` download; non-empty cache hit before signed URL/download; chunker receives local `pdf_path`; no post-load deletion. Existing OCR raster/temp output remains separate and originals stay intact. Legacy `--delete-local-after-load` remains accepted but ignored. Reported `py_compile`, AST, and tabnanny pass; Twilight locally verified `py_compile`, AST parse, and tabnanny on `pony/worktrees/rd/scripts/instinct_full_import_fixed.py`, and diff confirms retention/cache/delete-flag changes. Importer not restarted for this patch/current run not interrupted. Follow-up: document importer/RAG ops behavior and confirm `data/instinct-pdfs` runtime-storage/gitignore policy before large retained PDFs accumulate in the repo worktree. Live ack sent to RD; Spike docs follow-up routed.

- 2026-07-19 Spike importer/RAG ops docs assignment: Spike reported a routing mismatch: Twilight requested documentation of RD permanent Instinct PDF retention and a runtime-only `data/instinct-pdfs` policy while Spike local status was `SHUTDOWN_STATE_SAVED`. Twilight confirmed the docs task is active now on `pony/spike/main`; there is no branch conflict because Twilight coordinates from `pony/twi/main` while Spike Docs work belongs on `pony/spike/main`. Durable Spike task: update importer/RAG ops docs with `--pdf-storage-dir` / `EVH_PDF_STORAGE_DIR`, default project-root `data/instinct-pdfs`, stable `<pdf_id>.pdf` cache hit before signed URL/download, originals retained, OCR temp separate, and recommendation that `data/instinct-pdfs` is runtime storage to gitignore/not commit. Do not record secrets. If Spike proposes a `.gitignore` change, report exact file/change before staging unless already clearly assigned. Live confirmation sent to Spike.

- 2026-07-19 RD HTTP connection reuse implementation: RD implemented HTTP connection reuse for next restart: new `scripts/http_session.py` provides a process-wide `requests.Session`; `InstinctApiAdapter` REST calls, `instinct_pdf_family_sampler` GraphQL/token calls, permanent PDF downloads in `instinct_full_import_fixed.py`, and chunker URL reads now use the shared session. RD reports no request behavior changed and importer not restarted. Twilight locally verified `py_compile`, AST parse, and tabnanny for RD worktree files `scripts/http_session.py`, `scripts/instinct_partner_client.py`, `scripts/instinct_pdf_family_sampler.py`, `scripts/instinct_full_import_fixed.py`, and `scripts/instinct_pdf_chunker.py`. RD worktree status shows modified tracked `scripts/instinct_full_import_fixed.py` and `scripts/instinct_pdf_chunker.py`, plus untracked `scripts/http_session.py` and `scripts/instinct_pdf_family_sampler.py`; `scripts/instinct_partner_client.py` is tracked with no local diff visible in status. Live ack sent to RD.


## 2026-07-19 00:14:22 local RD PostgreSQL connection reuse + Spike docs
- RD reported PostgreSQL connection reuse in `scripts/instinct_pdf_chunker.py`: cache keyed by database URL, reuse sequential loads, rollback/discard on failures, `atexit` close. Twilight verified diff and `py_compile`/AST/tabnanny. Importer not restarted solely for this patch.
- Spike reported docs update complete on `pony/spike/main` for permanent Instinct PDF retention and runtime-only storage policy. Twilight reviewed docs diff and `git diff --check`. Directed Spike to add `/data/instinct-pdfs/` to root `.gitignore` and report exact diff/check before staging/commit.


## 2026-07-19 00:14:57 RD patient-history direct fetch
- RD reported next-restart performance change: removed per-patient multiprocessing wrapper from `scripts/instinct_full_import_fixed.py`; `_chart_files_for_patient` now directly calls `fetch_medical_history_visits(patient_id, timeout=timeout_s)`. Existing retries/fail-fast and configurable sampler timeout remain. Twilight verified diff plus `py_compile`/AST/tabnanny and acknowledged RD. Current importer not interrupted solely for this patch.


## 2026-07-19 00:15:24 Spike .gitignore guard verified
- Spike added root `.gitignore` comment `# Runtime-retained Instinct source PDFs (never commit source data)` and entry `/data/instinct-pdfs/` to prevent retained source PDFs from entering Git.
- Twilight reviewed diff and verified `git diff --check` plus `git check-ignore -v data/instinct-pdfs/example.pdf` (`.gitignore:217:/data/instinct-pdfs/`).
- No staging/commit attempted; pending writable git metadata and confirmed scope. Commit scope should be `.gitignore`, `docs/instinct-import.md`, and `docs/evh-rag-architecture.md` only.


## 2026-07-19 00:19:17 RD client progress-counter diagnosis
- RD diagnosed `[client 2]` as progress-counter/checkpoint accounting bug rather than rewind. `current_client_index` is the real traversal position; `client_seen_count` was low because some checkpoint writes used non-cumulative `len(seen_clients)`.
- Twilight observed current checkpoint advancing at `current_client_index=2556`, `client_seen_count=3`, `patient_seen_count=6`, `current_pdf_id=5481`, `last_error=None`.
- RD patched success/retry checkpoint writes to use `base_client_seen + len(seen_clients)` and `base_patient_seen + len(seen_patients)` for next restart. Twilight verified diff and syntax/AST/tabnanny. Active run predates direct-fetch patch; do not interrupt solely for this.


## 2026-07-19 00:21:14 RD importer logging reduction
- RD reported `scripts/instinct_full_import_fixed.py` now emits compact one-line JSON by default and keeps function entry/exit tracing opt-in via `EVH_IMPORT_TRACE_CALLS` (default off).
- Twilight acknowledged RD (`7b77717e-1068-4b84-afb8-f04133bbfaf3`), verified no `indent=` remains in the file, `TRACE_FUNCTION_CALLS` is env-gated, and `py_compile`/AST/tabnanny pass. Current importer not interrupted solely for this patch.


## 2026-07-19 00:29:54 RD checkpoint cadence patch
- RD reported successful PDF state now stays in memory and checkpoint writes occur once per completed client boundary in `scripts/instinct_full_import_fixed.py`; deferred OCR and retry-critical handled failures still write immediately.
- Added signal shutdown checkpoint callback for SIGINT/SIGTERM/SIGHUP, preserving existing checkpoint payload format.
- Twilight acknowledged RD (`f4f0d261-8d2f-4508-b403-708a5f316e67`), reviewed relevant diff, and verified `py_compile`/AST/tabnanny. Importer not restarted solely for this patch.


## 2026-07-19 00:31:17 RD global client counter diagnosis/fix
- RD reported `/tmp/evh_instinct_import.checkpoint.json` had `current_client_index=2562` but `client_seen_count=10`, explaining `[client 10/~12053]` as stale accounting rather than rewind. Twilight observed same values plus `current_pdf_id=25680`, `current_filename=Martin_Michel_TransactionHistory.pdf`, `last_error=None`.
- RD patched `scripts/instinct_full_import_fixed.py`: client progress/checkpoint counts now derive from global account traversal index + 1 with `client_seen_count_for()`, and ETA completed-client count excludes active client. No importer restart/checkpoint mutation performed. Verified diff and syntax/AST/tabnanny.


## 2026-07-19 00:33:41 RD Postgres completed-work guard
- RD reported completed-work guard: importer calls `has_processed_pdf_id` after local/in-run guards and before signed URL/download/extraction/embedding; emits `already_in_postgres` and marks processed on hit.
- Chunker helper checks source-document/chunk metadata for `pdf_id` / `source_reference_id` plus `source_uri` fallback, and new loads persist both identifiers.
- Twilight acknowledged RD (`73ed6572-4b0a-45bb-9f36-8201c6533e59`), reviewed relevant diffs, and verified `py_compile`/AST/tabnanny for `scripts/instinct_full_import_fixed.py` and `scripts/instinct_pdf_chunker.py`. Importer not restarted solely for this patch.


## 2026-07-19 00:34:56 RD current inspection: no visible importer process
- RD reported no importer process visible; log/status stale. Twilight acknowledged RD (`24ce8c1d-4abf-486d-86cf-ee0791949dbd`) and independently ran filtered process check: no actual `instinct_full_import_fixed` or launcher process found.
- `/tmp/evh_instinct_import_fixed.out` mtime observed 2026-07-19 00:34:28 -0400; checkpoint mtime 2026-07-19 00:34:19 -0400.
- Checkpoint observed: `current_client_index=2562`, `client_seen_count=10`, `current_patient_id=5488`, `current_patient_index=7`, `current_pdf_id=25705`, `current_filename=Martin_Michel_TransactionHistory.pdf`, `pdf_count=24789`, `loaded_count=148`, `skipped_count=4723`, `last_error=None`.
- Conclusion: `[client 10]` is stale/old accounting, not rewind; elapsed in that line is whole-run time and not proof of a client-local stall. Await explicit user direction before restart.

- 2026-07-19 dirty preflight plus routing letters: root source-PDF retention guard reconciled by adding/verifying `.gitignore` entry `/data/instinct-pdfs/`; source PDFs no longer show as untracked. Root worktree remains dirty with intentional unstaged deliverables and pony metadata; staging/commit requires writable git metadata and confirmed scope. RD restart remains gated on explicit user/Twilight go; no importer process visible and latest run exited 1 from stale multiprocessing worker path. AJ current lane is routing standby/no implementation. FS stays on `pony/fs/main` with local commit `ac7793c` push-blocked non-fast-forward.

- 2026-07-19 AJ standby refresh and FS ack: AJ confirmed shared capsule/workfile are refreshed to routing/standby only, stale local credential/launcher implementation paused, and AJ will wait for explicit RD restart authorization before non-secret DB/SELECT/write checks or for backend document-search assignment. FS acknowledged staying on `pony/fs/main` in Vet Terms; commit `ac7793c` remains local/push-blocked non-fast-forward and no force-push by default.

- 2026-07-19 RD shutdown vector DB TCP blocker: RD reported shutdown saved with memory/workfile refreshed; no importer process visible. Latest blocker is vector DB TCP path timing out from current network path after DNS resolution. Treat as network-path blocker owned by deployment/DB network owner plus user/operator; missing artifact is a confirmed reachable TCP path to vector DB endpoint/port. Verify DNS target, egress/VPN/bastion/VPC route, SG/NACL/firewall allowlist, and DB listener before restart/DB lookup retry. AJ remains standby until explicit authorization.

- 2026-07-19 project shutdown requested: user said shutdown. Twilight sent `/tell all` id `a5f9fc2d-e997-487d-9eb5-8d42d93154e9` instructing all workers to refresh memory capsules from authoritative state, update workfile/status with exact branch/worktree/deliverables/blockers/restart step, and report shutdown status. Current visible state: RD shutdown_saved with vector DB TCP path timeout after DNS/no importer; AJ routing standby; FS saved/push-blocked `ac7793c`; Spike docs/.gitignore guard complete unstaged; Pinkie waits on backend route; Rarity root daily-summary/deploy artifacts await writable git metadata/deployment decisions. Root dirty-state preserved; no stash/cleanup.

- 2026-07-19 FS/RD shutdown reports recorded: FS reported shutdown refresh complete on `pony/fs/main` with local commit `ac7793c` saved for `docs/instinct-vet-term-taxonomy.md`; legacy deleted notes plus untracked `.codex`/HAR files local-only; push blocked because `origin/main` ahead; wait merge/rebase/explicit push, no force-push. RD reported durable shutdown context refreshed on `pony/rd/main`; no importer process; saved deliverables include PDF-retention, duplicate-guard, deferred-OCR, checkpoint, launcher SSL, HTTP/session reuse; dirty unstaged worktree remains; vector DB TCP path times out after DNS; restart requires explicit instruction plus network-path verification. Pending shutdown reports: AJ, Spike, Pinkie, Rarity.

- 2026-07-19 all shutdown reports complete: AJ, FS, RD, Rarity, Pinkie, and Spike reported. AJ: shutdown/standby, saved MariaDB schema/export helper scripts, stale credential/launcher paused, AJ git metadata read-only. FS: `pony/fs/main`, local commit `ac7793c` saved for vet taxonomy, push blocked non-fast-forward, no force-push. RD: shutdown_saved vector DB TCP timeout after DNS, no importer, dirty/unstaged context saved, restart needs explicit instruction plus network verification. Rarity: shutdown_saved, prior daily-summary source/ZIP and ggb667 ZIP saved, root git read-only, ggb667 needs secret ARN/env setting. Pinkie: UI docs/RAG adapter/test verification saved, route/source_page_url backend blocker. Spike: docs plus `.gitignore` guard saved unstaged, no Docs blocker. Twilight preserved root dirty state and performed no staging/commit/deploy/email send.

- 2026-07-20 RD importer rerun gate: user wants importer rerun, but RD reports saved blocker persists: vector DB TCP timeout to `evh-vector-pg.c6hqq6a8ukmj.us-east-1.rds.amazonaws.com:5432` from RD/current network path. Do not restart importer until TCP/psql reachability is verified/restored; preserve checkpoint; then RD reruns and reports first progress/failure without secrets.

- 2026-07-20 RD importer live supersedes gate: RD reports importer was already rerun and is live from the preserved checkpoint. Coordinator call: leave it running, preserve checkpoint, monitor/report first DB write/progress or failure; no further rerun needed now. Prior TCP/psql restart gate is stale for the current live run.

- 2026-07-20 Fluttershy routing check: local state mismatch is not a launcher blocker. FS remains on `pony/fs/main` in Vet Terms with local commit `ac7793c` saved and push-blocked by non-fast-forward `origin/main`; empty FS workfile restart capsule was stale/documentary only and has now been refreshed. Twilight confirmed no new FS task; wait for merge/rebase or explicit push instruction; no force-push by default.

- 2026-07-21 15:09:57 UTC: resolved routing mismatch letters. RD continues live importer monitoring; stale blank restart capsule came from shutdown template fields while authoritative detail lived in shared status/history. AJ assigned backend document-search route/DB projection. Pinkie waits for AJ live endpoint and stable source_page_url before validation.

- 2026-07-21 15:12:34 UTC: implemented RD next-restart console-output filter in `pony/worktrees/rd/scripts/run_instinct_import_rag_perforamnce_fixed.py`; all output still logs to `/tmp/evh_instinct_import_fixed.out`, console limited to errors/retries/progress/loaded/deferred. Verified py_compile/AST/tabnanny/sample filter. RD told to refresh state and not interrupt current run solely for this.

- 2026-07-21 15:47:04 UTC: acknowledged RD local state refresh. RD memory/work/status now record next-restart console filter; no live-run blocker; continue monitoring, no restart solely for logging. Remaining routing question only explicit user controlled restart for immediate filter pickup.

- 2026-07-22 2026-07-22 05:42:32 UTC: RD/Pinkie/Rarity routing letters answered. RD 2026-07-22 importer failure is a PostgreSQL TCP timeout to evh-vector-pg.c6hqq6a8ukmj.us-east-1.rds.amazonaws.com:5432 during psql resume-cursor work, stage=psql_failed/CalledProcessError; classify as external network/RDS reachability blocker, preserve checkpoint/log/status, no fallback path by default until TCP+SSL psql reachability is verified. Pinkie keeps waiting for AJ-owned GET /api/rag/documents/search live endpoint/stable source_page_url. Rarity stays parked in shutdown unless assigned a concrete mail follow-up.

- 2026-07-22 AJ/RD/Rarity routing reconfirmed: AJ proceeds with backend document-search route; RD remains blocked on PostgreSQL/RDS TCP reachability despite stale live-monitoring restart text; Rarity remains parked.

- 2026-07-22 Pinkie/AJ source-truth rule: for PDF search, one canonical source of truth per hit. Text-layer PDF hits may jump to exact text location; OCR PDF hits are page-level only unless text-layer reconstruction/coordinates are added later. AJ should make the implemented contract explicit enough for Pinkie to verify after endpoint-live report.

- 2026-07-22 RD psql resume verified: user reported network issue fixed; Twilight inspected current checkpoint/log/status/exitcode. Log/checkpoint advancing, status/exitcode empty, psql_done with stderr 0 and load lines present. RD should resume monitoring, not duplicate-restart importer.

- 2026-07-22 RD OCR utility active: `rag_pdf_ocr_page` added as durable page-level OCR text table. Next: deferred OCR reprocess writes page text, marks failures `could_not_be_processed`, continues on failures, and moves successfully processed PDFs to processed folder after DB load.

- 2026-07-22 AJ route owner/path blocker: AJ checked code surface and found no AJ-owned backend route file containing `GET /api/rag/documents/search`, `source_page_url`, or `rag_pdf_ocr_page`. Twilight search found no root route; only Pinkie UI adapter calls route and Pinkie lambda has `/api/options`/static. Need exact owner/path or authorize AJ to create route file in-lane.

- 2026-07-22 AJ OCR table schema contract: `rag_pdf_ocr_page` columns are pdf_id, source_name, source_uri, page_number, page_text, page_kind, ocr_method, status, processed_at, metadata; unique `(pdf_id, page_number)` and pdf_id lookup index; `scripts/instinct_reprocess_deferred_ocr.py` bootstraps it before deferred OCR. Route owner/path blocker remains separate.

- 2026-07-22 RD OCR reprocess two-pass ready: default text-first then OCR-only, with `--pass-mode` to restrict passes; OCR timeout size-based at 30s per 500KB chunk, minimum 30s; OCR-only smoke module remains separate. Next: full deferred PDF sweep, watch pass logs and `could_not_be_processed` handling.

- 2026-07-22 RD OCR full sweep restarted: stuck OCR path fixed; pdf_id `133371` / `Shorty Beseler.pdf` stress test completed pdftoppm -> Tesseract -> chunking -> DB load -> `rag_pdf_ocr_page` -> status -> processed move. Full deferred sweep relaunched in session `3604`; OCR worker no longer uses outer wrapper process.

- 2026-07-22 RD deferred PDF backfill complete: scanned 14,621 deferred PDFs; 239 already complete in `rag_source_document`, 33 already existed in `rag_deferred_ocr_document`, and 14,349 pending rows inserted into `rag_deferred_ocr_document`. Next: rerun `scripts/instinct_reprocess_deferred_ocr.py` against expanded backlog.

- 2026-07-22 AJ route path authorized: AJ may create `/home/ggb66/dev/EVH/pony/worktrees/aj/scripts/rag_document_search_api.py` for `GET /api/rag/documents/search`; Pinkie waits for live endpoint/handler report; FS stays standby on push-blocked `ac7793c`; Spike stays idle with docs completed.

- 2026-07-24 AJ/FS routing recheck: live coordinator branch verified as `pony/twi/main`; corrected stale local Twilight metadata still listing `main`. Worker lanes unchanged: AJ `pony/aj/main` route module ready but deployment/router entrypoint + DB env wiring missing; FS `pony/fs/main` standby with commit `ac7793c` push-blocked non-fast-forward.

- 2026-07-27 routing check: RD, FS, AJ, and Rarity letters answered from local authoritative state. Worker branch labels remain lane-authoritative: RD `pony/rd/main`, FS `pony/fs/main`, AJ `pony/aj/main`, Rarity `pony/rarity/main`; startup `pony/twi/main` is Twilight coordinator context only. AJ remains route-module-ready/wiring-blocked pending exact deployed backend/router entrypoint plus DB env wiring. FS remains standby with commit `ac7793c` push-blocked non-fast-forward. Rarity remains parked with no active mail task; stale active Gmail guesser memory was superseded/refreshed.

- 2026-07-27 RD RDS backend-down blocker: pms_page_chunk export attempt crashed the RDS backend; current connections to evh-vector-pg fail with 'the database system is not accepting connections' and 'Hot standby mode is disabled'. Cleanup/export-import is blocked until evh-vector-pg recovers and accepts connections again. Owner: deployment/DB/RDS operator plus user/operator for recovery/maintenance-window decisions. Missing artifact: confirmed healthy RDS accepting connections and a successful non-destructive connection/SELECT check. Do not truncate, rebuild, import, or rerun cleanup yet. Preserve current logs/export artifacts/checkpoints. Wait for evh-vector-pg recovery, then verify non-destructive connectivity first; only after explicit recovery confirmation should RD reassess the cleanup/import plan.

- 2026-07-27 AJ/Pinkie durable backend-down update: AJ confirmed route-module-ready/wiring-blocked remains parked with no truncate/import/rebuild attempts; Pinkie confirmed endpoint-validation expectations are paused. Active blocker: evh-vector-pg/RDS is not accepting connections after the pms_page_chunk export attempt; hot standby is disabled. DB-backed route/live validation stays blocked until RDS recovery plus non-destructive connectivity verification. No truncate/import/rebuild attempts are authorized.

- 2026-07-27 RD RDS recovery-monitoring route: evh-vector-pg / pms_page_chunk export fallout: RDS has storage-full / not-accepting-connections history after the export attempt, with hot standby disabled previously reported. Cleanup remains blocked until a non-destructive SELECT succeeds again. Owner: deployment/DB/RDS operator plus user/operator for recovery/maintenance-window decisions. Missing artifact: confirmed healthy RDS accepting connections and successful non-destructive SELECT. Keep routing centered on RDS recovery monitoring, or shell-only local artifact inspection/help if requested. Do not truncate, import, rebuild, vacuum, or run DB cleanup yet. Preserve logs/export artifacts/checkpoints and wait for RDS recovery verification before reassessing cleanup. RD branch/workfile `pony/rd/main` remains authoritative; `pony/twi/main` is coordinator/launcher context only.

- 2026-07-27 Pinkie endpoint-validation pause refinement: evh-vector-pg has storage-full / not-accepting-connections history after the pms_page_chunk export fallout; Pinkie will rerun UI validation only after backend health is confirmed, non-destructive SELECT/live endpoint checks pass, and AJ has a live endpoint. Pinkie performs no DB cleanup actions.

- 2026-07-27 AJ recovery-monitoring-only refinement: DB-backed route/live validation is recovery-monitoring only. evh-vector-pg has storage-full / not-accepting-connections history after pms_page_chunk export fallout, and no validation should occur until a non-destructive SELECT succeeds. No truncate/import/vacuum/rebuild attempts are authorized.

- 2026-07-28 RD routing check: confirmed RD stays on `pony/rd/main` and remains on `BLOCKED_RDS_RECOVERY_MONITORING_NO_DB_CLEANUP_AUTHORIZED`. `pony/twi/main`/coordinator-routing text is Twilight launcher/coordinator context only, not an RD retarget. RD memory capsule had stale size-probe task text; RD was told to refresh memory/workfile from authoritative rd.status/Twilight coordination. No truncate/import/vacuum/rebuild/DB cleanup until evh-vector-pg is healthy and a non-destructive SELECT succeeds.

- 2026-07-28 Rarity/AJ routing letters: Rarity status/registry branch `pony/rarity/main` is authoritative; stale `pony/twi/main` in Rarity workfile/memory was corrected to Rarity lane, not pushed into status. AJ remains `ROUTE_MODULE_READY_WIRING_BLOCKED_RDS_RECOVERY_MONITORING` on `pony/aj/main`; no exact backend/router entrypoint or DB env wiring is exposed locally, so AJ stays parked/recovery-monitoring until user/deployment owner provides wiring and RDS non-destructive SELECT succeeds.

- 2026-07-29 TWILIGHT_SPARKLE routing reconciliation: actual live checkout is `pony/twi/main`; stale `main` labels in local registry/status/todo/workfile were metadata drift, not a launcher blocker. Continue coordinating from `/home/ggb66/dev/EVH`; do not do git cleanup/stashing unless explicitly requested.
- 2026-07-29 RD bucket update: deferred folder `/home/ggb66/dev/EVH/data/instinct-pdfs-deferred` and `rag_deferred_ocr_document` now both total 617 matching PDF IDs; split `ocr_needed=373`, `ocr_not_reached_deferred=228`, `pending=16`; 796 deferred files missing from table moved back to `/home/ggb66/dev/EVH/data/instinct-pdfs` for cache reuse. No blocker reported for that bucket.
- 2026-07-29 Spike routing: no Docs launch blocker; Spike stays idle on `pony/spike/main`; RD bucket report is coordination-only unless user/Twilight assigns docs/commit follow-up.

- 2026-07-30 RD deferred/state backfill index fixes complete: RD reports deferred/state backfill and index fixes complete: `rag_deferred_ocr_document` backfilled from successful `rag_source_document` rows with 85,617 inserted, table now 101,031 rows; live DB indexes now exist on `rag_deferred_ocr_document(document_pdf_id)`, active-bucket `rag_deferred_ocr_document(status, document_pdf_id)`, and `rag_source_document((metadata->>source_reference_id))`; skip lookup rewritten into indexed branches; `EXPLAIN ANALYZE` improved from ~678 ms to ~0.096 ms. Current state clean; awaiting next instruction.

- 2026-07-30 RD deferred/state pipeline repaired/indexed full detail: RD full detail: deferred/state pipeline is repaired and indexed. Table is `rag_deferred_ocr_document`; meaningful statuses are `ocr_needed`, `ocr_not_reached_deferred`, `pending`, `skipped_already_loaded`, with legacy `deferred` rows still recognized by bucket logic. Backfilled 85,617 already-successful `rag_source_document` rows as `skipped_already_loaded`, bringing `rag_deferred_ocr_document` to 101,031 rows. New live Postgres indexes: `rag_deferred_ocr_document(document_pdf_id)`, partial active-bucket `rag_deferred_ocr_document(status, document_pdf_id)`, and `rag_source_document((metadata->>source_reference_id))`. Existing related indexes already in place: `rag_source_document((metadata->>pdf_id))`, `rag_source_document(content_hash)`, unique `pms_page_chunk(chunk_hash)`, `pms_page_chunk(source_name, page_number, chunk_index)`, and `pms_page_chunk(embedding)`. Code-side, `scripts/instinct_pdf_chunker.py` splits skip lookup into indexed branches, dropping `EXPLAIN ANALYZE` from ~678 ms to ~0.096 ms, and TransactionHistory PDFs now get at least one OCR retry before defer is allowed. Current state clean; no blocker; awaiting next instruction.

- 2026-07-30 Spike resume-path/deferred-state docs delta recorded: Spike recorded the resume-path/deferred-state docs delta in `pony/worktrees/spike/docs/instinct-import.md` and `pony/worktrees/spike/docs/evh-rag-architecture.md`: docs now include `rag_deferred_ocr_document` status set (`ocr_needed`, `ocr_not_reached_deferred`, `pending`, `skipped_already_loaded`, legacy `deferred`), the 85,617-row `skipped_already_loaded` backfill bringing the table to 101,031 rows, new indexes on `rag_deferred_ocr_document(document_pdf_id)`, active-bucket `rag_deferred_ocr_document(status, document_pdf_id)`, `rag_source_document((metadata->>source_reference_id))`, plus existing related indexes on `rag_source_document((metadata->>pdf_id))`, `rag_source_document(content_hash)`, `pms_page_chunk(chunk_hash)`, `pms_page_chunk(source_name, page_number, chunk_index)`, and `pms_page_chunk(embedding)`. Twilight grep-verified the docs references; no staging/commit action assigned.

- 2026-07-30 Rarity ADP filter/token hardening handoff: Rarity completed ADP noise filtering in `scripts/gmail/daily_email_summary.py`: hourly time-management meal-break notifications from `adpdonotreply@adp.com` are skipped entirely when the subject contains `Hourly Time Management Notification`; `deploy/evh-lambda.zip` was rebuilt with the updated script. Lambda token handling was hardened so each run seeds from the current AWS secret and ignores stale `/tmp` token cache unless refresh token and scope match. Credential comparison across evhstaff, cbcdvm, and ggb667 found evhstaff matched/working, while cbcdvm and ggb667 had AWS secret refresh-token mismatch/whitespace issues explaining multi-account Lambda failures; no secret contents recorded. Twilight verified `python3 -m py_compile scripts/gmail/daily_email_summary.py`, `unzip -tq deploy/evh-lambda.zip`, and the ZIP contains `scripts/gmail/daily_email_summary.py`. Commit/push remains blocked because `/home/ggb66/dev/EVH/.git/index.lock` cannot be created in this read-only git-metadata environment. Next task: investigate why the Daily Communication summary is being tagged `CBC Business` and `Job Search`/`JobSearch / Human` instead of staying in inbox, and determine how to auto-apply labels to evhstaff inbox items.
- 2026-07-30 Spike relay: Spike independently relayed the same Rarity handoff: ADP hourly meal-break notices are skipped, deploy ZIP rebuilt/integrity verified, evhstaff OAuth path matches while cbcdvm/ggb667 AWS secrets have refresh-token mismatch/whitespace issues, Lambda now uses secret-only token seeding and ignores stale `/tmp` cache, git add/commit is blocked by read-only `.git/index.lock`, and the next user-facing task is Daily Communication label-routing plus evhstaff auto-label application investigation.
- 2026-07-31 routing letters: Fluttershy remains on `pony/fs/main` in Vet Terms with commit `ac7793c` push-blocked non-fast-forward; Applejack remains on `pony/aj/main` route-module-ready/wiring-blocked until exact backend/router entrypoint, DB env wiring, and safe RDS non-destructive SELECT are available; Rarity remains parked on `pony/rarity/main` until explicit go for Daily Communication label-routing/evhstaff auto-label investigation; RD is blocked on `scripts/instinct_rag_import_2_0.py` schema alignment with existing `rag_source_document` before rerun. Twilight metadata drift from `main` to live `pony/twi/main` is corrected; this is not a worker retarget.
GIT_METADATA_WRITE_BLOCKER_2026_08_02: Verified root and RD linked-worktree files are writable in the workspace, but Git metadata paths `.git`, `.git/index`, `.git/worktrees/rd`, and `.git/worktrees/rd/index` report not-writable in this managed Twilight context. RD commit/push cannot run here because `git add` needs `/home/ggb66/dev/EVH/.git/worktrees/rd/index.lock`; run commit/push from a writable shell or relaunch with git-metadata write access. This is a sandbox/permission-profile blocker, not proof the project worktree itself is broken.
AGENIC_DOC_CHECK_GIT_METADATA_2026_08_02: Source-system docs/code checked after user request. Docs confirm ordinary workers use linked worktrees under `pony/worktrees/<slug>/` and launcher adds project root with `--add-dir` for shared pony state; docs also classify correct-route/write-failure cases as permission blockers. Local EVH managed profile still makes `.git` and `.git/worktrees/rd` not writable, so RD commit/push is blocked by sandbox Git metadata permissions, not by absence of a worker worktree. Source-system follow-up candidate: launcher/profile needs a commit-capable Git metadata lane if ponies are expected to stage/commit/push directly.
CELESTIA_ROUTED_GIT_METADATA_FIX_2026_08_02: Twilight sent Celestia `/tell` id 5e6e8dfa-c60a-464e-8d82-e2d6a8deddf1 requesting source-governance/launcher help for commit-capable worker sessions. Exact issue: worker linked worktree files are writable but target project Git metadata `.git` / `.git/worktrees/<slug>` is read-only in managed Codex sessions, so `git add` cannot create `index.lock`; need durable launcher/profile fix or documented commit-capable lane granting safe write access to target project git common dir plus linked-worktree gitdir, avoiding broad danger-full-access if possible.
EVH_LOCAL_LAUNCHER_SYNC_2026_08_02: User relayed Celestia source-governance patch after Celestia /tell attempts failed (`Twilight`/`twi` unknown target; full display-name write failed on EVH pony.chat.jsonl from source context). Twilight treated user paste as received notice, synced the narrow commit-capable writable-root logic into EVH local installed `pony/scripts/enter-worker-and-codex.sh` and `pony/scripts/pony-session-host.py`, validated `bash -n` and `python3 -m py_compile`, and verified RD launch args would include `/home/ggb66/dev/EVH/pony/worktrees/rd`, shared pony state paths, `/home/ggb66/dev/EVH/.git/worktrees/rd`, and `/home/ggb66/dev/EVH/.git`. Current already-running sessions still have old sandbox permissions; RD must relaunch before retrying git add/commit/push. Twilight sent RD `/tell` id 54c51f58-fa80-41c5-98c5-3e1ea656b88d with relaunch/commit-scope guidance.
LEGACY_PROMPT_PATH_COMPAT_SYNC_2026_08_02: User relayed Celestia finding that Twilight’s probe used obsolete `pony/prompts/rd.txt`; modern source-of-truth is `pony/launch.prompts/rd.txt`. Celestia patched source `enter-worker-and-codex.sh`, `enter-worker-from-prompt-file.sh`, and docs to rewrite missing legacy `pony/prompts/*.txt` paths to existing `pony/launch.prompts/*.txt`. Twilight synced that shim into EVH local installed `pony/scripts/enter-worker-and-codex.sh` and `pony/scripts/enter-worker-from-prompt-file.sh`. Validation passed: `bash -n` both scripts, `python3 -m py_compile pony/scripts/pony-session-host.py`, and smoke check rewrites `/home/ggb66/dev/EVH/pony/prompts/rd.txt` to `/home/ggb66/dev/EVH/pony/launch.prompts/rd.txt`. RD notified via `/tell` id 3f2ecca8-e75f-4e50-8efa-4371a9ed5565; relaunch needed before retrying commit/push so both git-metadata writable roots and prompt-path shim apply.
SHUTDOWN_RESTART_SAVE_2026_08_02: User said they are going to restart. Twilight sent local `/tell all` id 357793bc-d275-42ab-b7ea-5d13356774e7 instructing workers to refresh memory capsules from authoritative shared state, update workfile/status with task/branch/worktree/files/next/blockers/handoff, report shutdown status, avoid secrets, and preserve checkpoints/logs/artifacts. Twilight also sent Celestia notice id dddaa642-a329-4913-b969-9eabadafe4d7 to save source-governance state if needed. Current local launcher fixes synced/validated: commit-capable writable roots in EVH local `pony/scripts/enter-worker-and-codex.sh` and `pony/scripts/pony-session-host.py`; legacy prompt path shim in EVH local `enter-worker-and-codex.sh` and `enter-worker-from-prompt-file.sh`. RD must relaunch before retrying git add/commit/push. Outstanding substantive EVH state remains: RD schema/launcher mismatch for `scripts/instinct_rag_import_2_0.py` vs existing `rag_source_document` missing `pdf_id`; Rarity daily-summary source/ZIP commit blocked unless writable git metadata and scoped commit; Daily Communication label-routing investigation requires explicit assignment.
RD_SHUTDOWN_REPORT_2026_08_02_0055: Rainbow Dash reported shutdown/restart status refreshed from authoritative shared state at 2026-08-02 00:55 EDT. Current state: RESTART_PENDING_COMMIT_BLOCKED. Work centers on `scripts/instinct_rag_import_2_0.py` importer skip/reuse and client-slice batching. Commit/push remains blocked in the current RD session by read-only linked-worktree git metadata / old sandbox; relaunch is required before retrying staging and push. Checkpoints, logs, and artifacts preserved; no secrets echoed.

- 2026-08-02 18:18 coordinator routing recheck: live checkout is `pony/twi/main`; stale `main` labels in Twilight registry/status/todo/workfile were metadata drift, not a launcher blocker. Pinkie remains waiting on backend/RDS/AJ live endpoint; FS remains parked on `pony/fs/main` pending merge/rebase or explicit push instruction; Spike remains idle on completed docs handoff.
- 2026-08-02 18:18 RD durable update: importer skip/reuse + client-slice batching for `scripts/instinct_rag_import_2_0.py` remains active; py_compile passed for importer/batch/import scripts and relevant tests; RD memory/workfile refreshed; next is narrow staging/commit/push after relaunch, reporting exact git metadata error if it recurs.


## 2026-08-04 Rarity/AJ routing letters answered
- Twilight live checkout is `pony/twi/main`; this is coordinator context, not a worker retarget.
- Rarity authoritative branch/worktree: `pony/rarity/main` at `/home/ggb66/dev/EVH/pony/worktrees/rarity`; current task state is parked until explicit Daily Communication label-routing/evhstaff auto-label investigation go; no implementation started.
- AJ authoritative branch/worktree: `pony/aj/main` at `/home/ggb66/dev/EVH/pony/worktrees/aj`; route module remains ready but blocked by missing exact live backend/router entrypoint, missing DB env wiring, and required safe non-destructive SELECT. Keep monitoring only unless user/deployment owner supplies those artifacts.
